# Development Environment

This project uses a virtualenv located at `~/.virtualenvs/agentic-mvp/`.

When running Python commands, use:
- `~/.virtualenvs/agentic-mvp/bin/python`
- `~/.virtualenvs/agentic-mvp/bin/pip`
